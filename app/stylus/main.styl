@import 'nib'

global-reset()

.buttons
  .button
    &.buy
      button orange
    &.wish
      button blue

.container
  +above(1023px)
    lost-center 1020px
  .product
    +between(480px, 767px)
      lost-column 1/2
    +between(767px, 1023px)
      lost-column 1/3
    +above(1024px)
      lost-column 1/4
